[pytest]
pythonpath = .
testpaths = server/src/tests
asyncio_mode = auto
norecursedirs = .git .* build dist __pycache__ client common
filterwarnings =
    ignore:Importing from 'glide\.:DeprecationWarning
markers =
    integration: marks tests as integration tests (require database and WebSocket)
